{
  "name": "@openparachute/surface-monorepo",
  "version": "0.2.3",
  "private": true,
  "description": "Workspace root for parachute-surface \u2014 host module (@openparachute/surface) + shared client library (@openparachute/surface-client).",
  "license": "AGPL-3.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "web/admin"
  ],
  "scripts": {
    "build": "bun run --filter '@openparachute/surface-client' build && bun run --filter '@openparachute/surface-render' build && bun run --filter '@openparachute/surface' build && bun run --filter '@openparachute/pebble-config' build",
    "build:admin": "bun run --filter '@openparachute/surface-admin-ui' build",
    "test": "bun test packages/surface-host/src/ packages/surface-client/src/ packages/pebble-config/src/ && bun run --filter \"@openparachute/notes-ui\" test && bun run --filter \"@openparachute/surface-render\" test",
    "test:host": "bun test packages/surface-host/src/",
    "test:client": "bun test packages/surface-client/src/",
    "test:render": "bun run --filter '@openparachute/surface-render' test",
    "test:admin": "bun run --filter '@openparachute/surface-admin-ui' test",
    "test:all": "bun run test && cd web/admin && bun run test",
    "typecheck": "cd packages/surface-host && tsc --noEmit && cd ../surface-client && tsc --noEmit && cd ../surface-render && tsc --noEmit && cd ../notes-ui && tsc -b --noEmit && cd ../pebble-config && tsc --noEmit",
    "build:render": "bun run --filter '@openparachute/surface-render' build",
    "typecheck:admin": "cd web/admin && bun run typecheck",
    "typecheck:all": "bun run typecheck && bun run typecheck:admin",
    "lint": "biome check .",
    "test:notes-ui": "bun run --filter \"@openparachute/notes-ui\" test",
    "build:notes-ui": "bun run --filter \"@openparachute/notes-ui\" build",
    "build:surface-host": "bun run --filter '@openparachute/surface' build",
    "build:surface-client": "bun run --filter '@openparachute/surface-client' build",
    "test:pebble-config": "bun run --filter '@openparachute/pebble-config' test",
    "build:pebble-config": "bun run --filter '@openparachute/pebble-config' build"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
