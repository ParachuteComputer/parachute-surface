{
  "name": "@openparachute/app-monorepo",
  "version": "0.2.0-rc.3",
  "private": true,
  "description": "Workspace root for parachute-app — host module (@openparachute/app) + shared client library (@openparachute/app-client).",
  "license": "AGPL-3.0",
  "type": "module",
  "workspaces": ["packages/*", "web/admin"],
  "scripts": {
    "build": "bun run --filter '@openparachute/app-client' build && bun run --filter '@openparachute/app' build",
    "build:app-host": "bun run --filter '@openparachute/app' build",
    "build:app-client": "bun run --filter '@openparachute/app-client' build",
    "build:admin": "bun run --filter '@openparachute/app-admin-ui' build",
    "test": "bun test packages/app-host/src/ packages/app-client/src/",
    "test:host": "bun test packages/app-host/src/",
    "test:client": "bun test packages/app-client/src/",
    "test:admin": "bun run --filter '@openparachute/app-admin-ui' test",
    "test:all": "bun test packages/app-host/src/ packages/app-client/src/ && cd web/admin && bun run test",
    "typecheck": "cd packages/app-host && tsc --noEmit && cd ../app-client && tsc --noEmit",
    "typecheck:admin": "cd web/admin && bun run typecheck",
    "typecheck:all": "bun run typecheck && bun run typecheck:admin",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
