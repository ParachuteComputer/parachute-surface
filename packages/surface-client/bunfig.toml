# Scope `bun test` (no args) to this package's src/ — same discipline as
# the app-host workspace. Without this, running `bun test` from the
# package root globs surrounding workspace dirs.

[test]
root = "src"
